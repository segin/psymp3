#ifndef SCROBBLE_H
#define SCROBBLE_H

class Scrobble : public track
{
    public:
        Scrobble(const track& rhs);
        virtual ~Scrobble();
    protected:
    private:
};

#endif // SCROBBLE_H
